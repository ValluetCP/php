<?php

// Création du formulaire, vérifier s'il s'affiche bien sur le navigateur
// Dans les name (nommé comme les attributs de la base de donnée)

/* debug (page de traitement): 
- appeler la fonction debugDie($_POST) dans function.php,
- l'écrire en dessous de if(ISSET). Il doit retourner un tableau associatif dans page action.php
-